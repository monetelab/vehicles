import { StyleSheet } from "react-native";

export const MyColors = {
  primary: "#790ee3",
  secondary: "#eb9706",
  background: "#EEEEEE",
};

export const MyStyles = StyleSheet.create({
  loading: {
    position: "absolute",
    bottom: 0,
    top: 0,
    right: 0,
    left: 0,
  },
});
